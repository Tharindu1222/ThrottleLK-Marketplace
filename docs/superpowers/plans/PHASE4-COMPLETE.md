# Phase 4 complete — Favourites + Compare

**Date:** 2026-09-11

## Shipped

- API: `GET/POST/DELETE /api/v1/favourites`, `GET /api/v1/favourites/ids`
- Listing detail: Save favourite + Add to compare
- `/en/account/favourites`
- `/en/compare` side-by-side table (max 3, localStorage)
- Floating compare tray when selection non-empty
- Header links for favourites + compare (when logged in)

## Verified

- Favourite add / list / delete via API
- Compare + favourites pages return 200
- Web build includes new routes

## Next

Phase 5 dealer showroom polish, or saved searches / notifications.
